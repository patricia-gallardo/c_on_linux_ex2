#include "csv.h"

#include <stdio.h>
#include <stdlib.h>

int convert(char * input_file, char * output_file) {
  printf("Reading from : %s\n", input_file);
  printf("Writing to   : %s\n", output_file);
  return EXIT_SUCCESS;
}
